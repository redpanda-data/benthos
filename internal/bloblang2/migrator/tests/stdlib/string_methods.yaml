description: "String methods: uppercase, lowercase, trim, trim_prefix, trim_suffix, has_prefix, has_suffix, split, replace_all, repeat"

tests:
  # --- .uppercase() ---

  - name: "uppercase basic"
    mapping: |
      root = "hello world".uppercase()
    output: "HELLO WORLD"

  - name: "uppercase already uppercase"
    mapping: |
      root = "HELLO".uppercase()
    output: "HELLO"

  - name: "uppercase empty string"
    mapping: |
      root = "".uppercase()
    output: ""

  - name: "uppercase mixed case"
    mapping: |
      root = "hElLo".uppercase()
    output: "HELLO"

  - name: "uppercase non-ascii"
    mapping: |
      root = "café".uppercase()
    output: "CAFÉ"

  # --- .lowercase() ---

  - name: "lowercase basic"
    mapping: |
      root = "HELLO WORLD".lowercase()
    output: "hello world"

  - name: "lowercase already lowercase"
    mapping: |
      root = "hello".lowercase()
    output: "hello"

  - name: "lowercase empty string"
    mapping: |
      root = "".lowercase()
    output: ""

  - name: "lowercase mixed case"
    mapping: |
      root = "HeLLo".lowercase()
    output: "hello"

  # --- .trim() ---

  - name: "trim spaces"
    mapping: |
      root = "  hello  ".trim()
    output: "hello"

  - name: "trim tabs and newlines"
    mapping: |
      root = "\t\nhello\n\t".trim()
    output: "hello"

  - name: "trim no whitespace unchanged"
    mapping: |
      root = "hello".trim()
    output: "hello"

  - name: "trim empty string"
    mapping: |
      root = "".trim()
    output: ""

  - name: "trim only whitespace"
    mapping: |
      root = "   \t\n  ".trim()
    output: ""

  # --- .trim_prefix() ---

  - name: "trim_prefix present"
    mapping: |
      root = "hello world".trim_prefix("hello ")
    output: "world"

  - name: "trim_prefix absent"
    mapping: |
      root = "hello world".trim_prefix("xyz")
    output: "hello world"

  - name: "trim_prefix empty prefix"
    mapping: |
      root = "hello".trim_prefix("")
    output: "hello"

  - name: "trim_prefix entire string"
    mapping: |
      root = "hello".trim_prefix("hello")
    output: ""

  - name: "trim_prefix only removes first occurrence"
    mapping: |
      root = "aaa".trim_prefix("a")
    output: "aa"

  # --- .trim_suffix() ---

  - name: "trim_suffix present"
    mapping: |
      root = "hello world".trim_suffix(" world")
    output: "hello"

  - name: "trim_suffix absent"
    mapping: |
      root = "hello world".trim_suffix("xyz")
    output: "hello world"

  - name: "trim_suffix empty suffix"
    mapping: |
      root = "hello".trim_suffix("")
    output: "hello"

  - name: "trim_suffix entire string"
    mapping: |
      root = "hello".trim_suffix("hello")
    output: ""

  # --- .has_prefix() ---

  - name: "has_prefix true"
    mapping: |
      root = "hello world".has_prefix("hello")
    output: true

  - name: "has_prefix false"
    mapping: |
      root = "hello world".has_prefix("world")
    output: false

  - name: "has_prefix empty prefix is always true"
    mapping: |
      root = "hello".has_prefix("")
    output: true

  - name: "has_prefix exact match"
    mapping: |
      root = "hello".has_prefix("hello")
    output: true

  # --- .has_suffix() ---

  - name: "has_suffix true"
    mapping: |
      root = "hello world".has_suffix("world")
    output: true

  - name: "has_suffix false"
    mapping: |
      root = "hello world".has_suffix("hello")
    output: false

  - name: "has_suffix empty suffix is always true"
    mapping: |
      root = "hello".has_suffix("")
    output: true

  - name: "has_suffix exact match"
    mapping: |
      root = "hello".has_suffix("hello")
    output: true

  # --- .split() ---

  - name: "split by comma"
    mapping: |
      root = "a,b,c".split(",")
    output: ["a", "b", "c"]

  - name: "split no delimiter found"
    mapping: |
      root = "hello".split(",")
    output: ["hello"]

  - name: "split empty string by comma"
    mapping: |
      root = "".split(",")
    output: [""]

  - name: "split empty string by empty string"
    mapping: |
      root = "".split("")
    output: []

  - name: "split by empty string produces codepoints"
    mapping: |
      root = "hello".split("")
    output: ["h", "e", "l", "l", "o"]

  - name: "split by empty string with non-ascii"
    mapping: |
      root = "café".split("")
    output: ["c", "a", "f", "é"]

  - name: "split with multi-char delimiter"
    mapping: |
      root = "one::two::three".split("::")
    output: ["one", "two", "three"]

  - name: "split trailing delimiter"
    mapping: |
      root = "a,b,".split(",")
    output: ["a", "b", ""]

  # --- .replace_all() ---

  - name: "replace_all basic"
    mapping: |
      root = "hello world".replace_all("world", "earth")
    output: "hello earth"

  - name: "replace_all multiple occurrences"
    mapping: |
      root = "aabaa".replace_all("a", "x")
    output: "xxbxx"

  - name: "replace_all no match"
    mapping: |
      root = "hello".replace_all("xyz", "abc")
    output: "hello"

  - name: "replace_all empty old with new"
    mapping: |
      root = "ab".replace_all("", "-")
    output: "-a-b-"

  - name: "replace_all remove substring"
    mapping: |
      root = "hello world".replace_all(" world", "")
    output: "hello"

  # --- .repeat() ---

  - name: "repeat basic"
    mapping: |
      root = "ab".repeat(3)
    output: "ababab"

  - name: "repeat zero times"
    mapping: |
      root = "hello".repeat(0)
    output: ""

  - name: "repeat one time"
    mapping: |
      root = "hello".repeat(1)
    output: "hello"

  - name: "repeat negative count is error"
    mapping: |
      root = "hello".repeat(-1)
    compile_error: "count"

  - name: "repeat empty string"
    mapping: |
      root = "".repeat(5)
    output: ""
